import 'package:flutter/material.dart';

class Screenbest extends StatelessWidget {
  const Screenbest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Best'),
    );
  }
}
